name: dartshell
description: Easily write and run shell-like scripts with dart.
version: 1.0.0
repository: https://github.com/komposten/dartshell

environment:
  sdk: ^3.12.2

# dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
